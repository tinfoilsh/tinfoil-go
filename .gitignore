# SDK build artifacts
Tinfoil.xcframework
Tinfoil.xcframework.zip
coverage.out
.env

# Cross-SDK conformance binary (built via `go build -o bin/tinfoil-conformance ./cmd/tinfoil-conformance/`)
/bin/

# Verifier build artifacts
verifier/TinfoilVerifier.xcframework
verifier/TinfoilVerifier.xcframework.zip
verifier/*.wasm
verifier/wasm/

# Go workspace (local dev only)
go.work
go.work.sum

# Common
cover.out
tuf-repo-cdn.sigstore.dev.json

# IDE
.idea/
